import express from 'express';
import {} from '../controller/storageController.js';

const router = express.Router();

// router.get("/", getProjects);
// router.delete("/:id", delProject);
// router.patch("/:id", updateProject);

export default router;
